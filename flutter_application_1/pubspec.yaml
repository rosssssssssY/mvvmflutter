name: flutter_application_1
description: A new Flutter project.

publish_to: 'none' # Remove this line if you plan to publish the package.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0  # Añade esta línea para usar el paquete provider

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true